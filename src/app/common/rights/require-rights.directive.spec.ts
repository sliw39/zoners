import { RequireRightsDirective } from './require-rights.directive';

describe('RequireRightsDirective', () => {
  it('should create an instance', () => {
    const directive = new RequireRightsDirective();
    expect(directive).toBeTruthy();
  });
});
