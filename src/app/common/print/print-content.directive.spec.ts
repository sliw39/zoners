import { PrintContentDirective } from './print-content.directive';

describe('FsContentDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintContentDirective(null);
    expect(directive).toBeTruthy();
  });
});
